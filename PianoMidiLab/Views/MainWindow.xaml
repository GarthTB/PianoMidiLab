<Window x:Class="PianoMidiLab.Views.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:tabs="clr-namespace:PianoMidiLab.Views.FXTabs"
    xmlns:vm="clr-namespace:PianoMidiLab.VMs"
    Title="PianoMidiLab 1.0.0"
    FontSize="14"
    UseLayoutRounding="True"
    ResizeMode="CanMinimize"
    SizeToContent="WidthAndHeight">
    <Window.DataContext>
        <vm:MainVM/>
    </Window.DataContext>
    <TabControl Padding="0">
        <TabItem Header="文件">
            <Grid Height="240"
                Width="420">
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition Height="51"/>
                </Grid.RowDefinitions>
                <ListBox AllowDrop="True"
                    ToolTip="支持拖放"
                    ItemsSource="{Binding Paths}"
                    SelectedItem="{Binding SelPath}"
                    Drop="DropMidiPaths"/>
                <Grid Row="1"
                    Margin="12,11">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition Width="12"/>
                        <ColumnDefinition/>
                        <ColumnDefinition Width="12"/>
                        <ColumnDefinition/>
                        <ColumnDefinition Width="12"/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>
                    <Button Content="添加"
                        Command="{Binding AddPathsCommand}"/>
                    <Button Grid.Column="2"
                        Content="移除"
                        Command="{Binding RemPathCommand}"/>
                    <Button Grid.Column="4"
                        Content="启动"
                        Command="{Binding RunCommand}"/>
                    <Button Grid.Column="6"
                        Content="打断"
                        Command="{Binding RunCancelCommand}"/>
                </Grid>
            </Grid>
        </TabItem>
        <TabItem Header="清理"
            IsEnabled="{Binding Idle}">
            <tabs:CleaningTab DataContext="{Binding CleaningTabVM}"/>
        </TabItem>
        <TabItem Header="力度映射"
            IsEnabled="{Binding Idle}">
            <tabs:MapVelTab DataContext="{Binding MapVelTabVM}"/>
        </TabItem>
        <TabItem Header="力度均衡"
            IsEnabled="{Binding Idle}">
            <tabs:PitchGainTab DataContext="{Binding PitchGainTabVM}"/>
        </TabItem>
    </TabControl>
</Window>